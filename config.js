let config = {};
config.port = 3100;

config.api = "http://localhost:";

config.host = "localhost";
config.user = "root";
config.password = "";
config.database =  "hux";

config.secretKey = "your_secret_key";

module.exports = config;

